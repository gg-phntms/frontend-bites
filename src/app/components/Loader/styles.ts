import { styled } from "@phntms/css-components";

import css from "./styles.module.scss";

export const LoaderContainer = styled("div", {
  css: css.LoaderContainer,
});

export const Button = styled("button", {
  css: css.Button,
});
