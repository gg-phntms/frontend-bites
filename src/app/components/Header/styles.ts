import { styled } from "@phntms/css-components";

import css from "./styles.module.scss";

export const HeaderContainer = styled("nav", {
  css: css.HeaderContainer,
});

export const Divider = styled("div", {
  css: css.Divider,
});
